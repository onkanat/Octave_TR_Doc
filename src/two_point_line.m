function line = two_point_line(start_point,end_point)
    % two_point_line
    % line = two_point_line(start_point,end_point)
    % start_point: start point
    % end_point: end point
    % line: line
    
    line = [start_point(1),start_point(2),end_point(1),end_point(2)];

end
